# DYS Backend - 환경변수 설정 예시
# GKE 환경에서 사용할 수 있는 기본 설정

# 애플리케이션 설정
APP_NAME=dys-backend
PORT=8000
ALLOWED_ORIGINS=*

# MongoDB 설정 (GKE 환경)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
DATABASE_NAME=dys-chatbot

# Supabase 인증 (GKE 환경)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
JWT_SECRET_KEY=your_jwt_secret_key

# OpenAI API (필수)
OPENAI_API_KEY=your_openai_api_key_here

# 네트워크 설정 (GKE 환경)
WEBSOCKET_HOST=your_gke_external_ip
WEBSOCKET_PORT=8001
CORS_ORIGINS=*,https://your-frontend-domain.com

# 파일 업로드 설정
MAX_UPLOAD_BYTES=33554432

# RunPod 설정 (선택사항)
RUNPOD_URL=https://runpod.io

# 로깅 설정
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# 모델 설정
MODEL_DOWNLOAD_URL=https://storage.googleapis.com/dys-model-storage/model.pth
MODEL_PATH=src/backend/models/ml_models/data/model.pth
